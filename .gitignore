# Editor / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Env
.env
.env.local

# Vendored Flutter SDK — local Windows dev only; CI uses subosito/flutter-action
flutter/

# Lokale Test-Uploads / Screenshots (nicht versionieren)
.pg-uploads/

# Flutter/Dart-Logs aus lokalen Runs (preview.ps1, flutter run)
*.log

# Claude-Code-CLI runtime state
.claude/scheduled_tasks.lock

# Build-generierte Files (freezed / json_serializable) — können jederzeit
# via `flutter pub run build_runner build` re-erzeugt werden.
*.freezed.dart
*.g.dart
