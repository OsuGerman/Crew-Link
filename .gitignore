# Editor / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Env
.env
.env.local

# Vendored Flutter SDK — local Windows dev only; CI uses subosito/flutter-action
flutter/

# Lokale Test-Uploads / Screenshots (nicht versionieren)
.pg-uploads/

# Flutter/Dart-Logs aus lokalen Runs (preview.ps1, flutter run)
*.log
